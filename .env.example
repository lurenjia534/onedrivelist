# This file serves as an example for the required environment variables.
# Copy this file to .env.local and fill in the values for your specific setup.

# --- Microsoft Entra ID (Azure AD) Provider Configuration ---
# Get these values from your app registration in the Microsoft Entra admin center.
AUTH_MICROSOFT_ENTRA_ID_ID="<Your Azure App ID>"
AUTH_MICROSOFT_ENTRA_ID_SECRET="<Your Azure App Client Secret>"

# This is the issuer URL for the Microsoft Identity Platform.
# For most multi-tenant applications, the following URL is correct.
AUTH_MICROSOFT_ENTRA_ID_ISSUER="https://login.microsoftonline.com/common/v2.0"


# --- Auth.js (NextAuth.js) Configuration ---
# This secret is used to encrypt the session JWT and should be kept private.
# Generate a new, strong secret for your project with the following command:
# openssl rand -base64 32
AUTH_SECRET="<Your generated 32-byte base64 secret>"

# This is the canonical URL of your application.
# For local development, it MUST be http://localhost:3000
# For production on Vercel, it MUST be your full deployment URL (e.g., https://onedrivelist-drab.vercel.app)
AUTH_URL="http://localhost:3000"

# ONEDRIVE_REFRESH_TOKEN
ONEDRIVE_REFRESH_TOKEN = ""
